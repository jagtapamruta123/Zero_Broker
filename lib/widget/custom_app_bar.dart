import 'package:flutter/material.dart';

class PreferredSizeWidge extends StatelessWidget {
  Icon icon;
  Function function;
  String title;
  PreferredSizeWidge({
    this.icon,
    this.function,
    this.title,
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return PreferredSizeWidge();
  }
}
